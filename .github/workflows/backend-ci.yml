name: Backend and Dashboard CI

on:
  push:
    paths:
      - "backend/**"
      - "frontend/**"
      - ".github/workflows/backend-ci.yml"
  pull_request:
    paths:
      - "backend/**"
      - "frontend/**"
      - ".github/workflows/backend-ci.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      BINANCE_MARKET_DATA_ENABLED: "false"
      LIVE_PAPER_ENABLED: "false"
      PAPER_PERSISTENCE_ENABLED: "false"
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: backend/requirements.txt
      - run: python -m pip install --upgrade pip
      - run: pip install -r requirements.txt
      - name: Backend including temporary SQLite recovery, crash and soak checks
        run: python -m pytest
      - run: python -m pip check
      - run: python scripts/export_dashboard_contract.py --check
      - run: python tests/export_dashboard_examples.py --check
      - run: python scripts/export_validation_report.py --check
      - run: python tests/export_validation_example.py --check
      - run: python tests/export_validation_example.py --medium --check

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v7
        with:
          node-version: "22.12.0"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run types
      - run: npm run types -- --check
      - run: git diff --exit-code -- src/types/generated.ts src/types/validation.generated.ts
      - run: npm test
      - run: npm run build
